<main class="main-wrapper col-md-9 ms-sm-auto py-4 col-lg-9 px-md-4 border-start">
    <div class="title-group mb-3">
        <h1 class="h2 mb-0">Distribuidores</h1>
    </div>


    <div class="row my-4">
        <div class="col-lg-12 col-12">
            <div class="custom-block bg-white">
                <ul class="nav nav-tabs" id="employeeTab" role="tablist">
                    <li class="nav-item" role="presentation">
                        <button class="nav-link active" id="add-employee-tab" data-bs-toggle="tab"
                            data-bs-target="#add-employee-tab-pane" type="button" role="tab"
                            aria-controls="add-employee-tab-pane" aria-selected="true">Editar Distribuidor</button>
                    </li>

                </ul>

                <div class="tab-content" id="employeeTabContent">
                    <!-- Editar Distribuidor -->
                    <div class="tab-pane fade show active" id="add-employee-tab-pane" role="tabpanel"
                        aria-labelledby="add-employee-tab">
                        <h6 class="mb-4">Editar Distribuidor</h6>

                        <!-- <form class="custom-form add-employee-form" action="/actualizar/<%= distribuidor.id %>"
                            method="POST" role="form">
                            <input class="form-control mb-3" type="number" name="usuario_id" id="usuario_id"
                                value="<%= distribuidor.usuario_id %>" placeholder="ID del Usuario" required>
                            <input class="form-control mb-3" type="text" name="empresa" id="empresa"
                                value="<%= distribuidor.empresa %>" placeholder="Empresa" required>
                            <input class="form-control mb-3" type="text" name="telefono" id="telefono"
                                value="<%= distribuidor.telefono %>" placeholder="Telefono" required>
                            <input class="form-control mb-3" type="text" name="direccion" id="direccion"
                                value="<%= distribuidor.direccion %>" placeholder="Direccion" required>
                            <input class="form-control mb-3" type="text" name="zona_cobertura" id="zona_cobertura"
                                value="<%= distribuidor.zona_cobertura %>" placeholder="Zona de Cobertura" required>

                            <div class="d-flex">

                                <button type="submit" class="form-control ms-2">Guardar</button>
                            </div>
                        </form> -->

                        <form class="custom-form add-employee-form" action="/actualizar/<%= distribuidor.id %>" method="POST">
                            <label for="cliente_id">Seleccionar Cliente:</label>
                            <select name="cliente_id" id="cliente_id" class="form-control mb-3" required>
                                <option value="">Seleccione un cliente</option>
                                <% if (clientes && clientes.length > 0) { %>
                                    <% clientes.forEach(cliente => { %>
                                        <option value="<%= cliente.id %>" <%= distribuidor.id_cliente === cliente.id ? 'selected' : '' %>>
                                            <%= cliente.nombre %>
                                        </option>
                                    <% }) %>
                                <% } else { %>
                                    <option value="" disabled>No hay clientes disponibles</option>
                                <% } %>
                            </select>
                        
                            <input class="form-control mb-3" type="number" name="usuario_id" id="usuario_id"
                                value="<%= distribuidor.usuario_id %>" placeholder="ID del Usuario" required>
                            <input class="form-control mb-3" type="text" name="empresa" id="empresa"
                                value="<%= distribuidor.empresa %>" placeholder="Empresa" required>
                            <input class="form-control mb-3" type="text" name="telefono" id="telefono"
                                value="<%= distribuidor.telefono %>" placeholder="Telefono" required>
                            <input class="form-control mb-3" type="text" name="direccion" id="direccion"
                                value="<%= distribuidor.direccion %>" placeholder="Direccion" required>
                            <input class="form-control mb-3" type="text" name="zona_cobertura" id="zona_cobertura"
                                value="<%= distribuidor.zona_cobertura %>" placeholder="Zona de Cobertura" required>
                        
                            <button type="submit" class="btn btn-primary">Actualizar</button>
                        </form>
                        





                    </div>


                </div>
            </div>
        </div>
    </div>



    <footer class="site-footer">
        <div class="container">
            <div class="row">

                <div class="col-lg-12 col-12">
                    <p class="copyright-text">Copyright © Mini Finance 2048
                        - Design: <a rel="sponsored" href="https://www.tooplate.com" target="_blank">Tooplate</a></p>
                </div>

            </div>
        </div>
    </footer>
</main>