<div class="container-fluid">
    <main class="main-wrapper col-md-9 ms-sm-auto py-4 col-lg-9 px-md-4 border-start">
        <div class="title-group mb-3">
            <h1 class="h2 mb-0">Clientes</h1>
        </div>

        <div class="row my-4">
            <div class="col-lg-12 col-12">
                <div class="custom-block bg-white">
                    <div class="row my-4">
                        <div class="col-lg-12 col-12">
                            <div class="custom-block bg-white ">
                                <div class="d-flex justify-content-between align-items-center">
                                    <h5 class="mb-5"> Lista de Clientes</h5>
                                    <ul class="d-flex list-unstyled mb-0">
                                        <li class="me-3">
                                            <a class="page-link active" href="clienteAgregar" aria-label="Next">
                                                + Agregar Cliente
                                            </a>
                                        </li>
                                    </ul>
                                </div>

                                <div class="table-responsive">
                                    <% if (mensaje) { %>
                                        <div class="alert alert-warning">
                                            <%= mensaje %>
                                        </div>
                                    <% } %>
                                    <table class="table table-striped">
                                        <thead>
                                            <tr>
                                                <th>Nombre</th>
                                                <th>Representante</th>
                                                <th>Telefono</th>
                                                <th>Correo</th>
                                                <th>Dirección</th>
                                                <th>Estado</th>
                                                <th>Acciones</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            <% if (clientes.length > 0) { %>
                                                <% clientes.forEach(cliente => { %>
                                                    <tr>
                                                        <td><%= cliente.nombre %></td>
                                                        <td><%= cliente.representante %></td>
                                                        <td><%= cliente.numero %></td>
                                                        <td><%= cliente.correo %></td>
                                                        <td><%= cliente.direccion %></td>
                                                        <td><%= cliente.estado ? 'Activo' : 'Inactivo' %></td>
                                                        <td>
                                                            <a href="clienteEditar/<%= cliente.id %>" class="btn btn-warning">Editar</a>
                                                            <form action="eliminar1/<%= cliente.id %>" method="POST" style="display:inline;">
                                                                <button type="submit" class="btn <%= cliente.estado ? 'btn-danger' : 'btn-success' %>">
                                                                    <%= cliente.estado ? 'Desactivar' : 'Activar' %>
                                                                </button>
                                                            </form>
                                                        </td>
                                                    </tr>
                                                <% }) %>
                                            <% } else { %>
                                                <tr>
                                                    <td colspan="7" class="text-center">No hay clientes registrados.</td>
                                                </tr>
                                            <% } %>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>

                    <nav aria-label="Page navigation example">
                        <ul class="pagination justify-content-center mb-0">
                            <li class="page-item">
                                <a class="page-link" href="#" aria-label="Previous">
                                    <span aria-hidden="true">Prev</span>
                                </a>
                            </li>

                            <li class="page-item active" aria-current="page">
                                <a class="page-link" href="#">1</a>
                            </li>

                            <li class="page-item">
                                <a class="page-link" href="#">2</a>
                            </li>

                            <li class="page-item">
                                <a class="page-link" href="#">3</a>
                            </li>

                            <li class="page-item">
                                <a class="page-link" href="#">4</a>
                            </li>

                            <li class="page-item">
                                <a class="page-link" href="#" aria-label="Next">
                                    <span aria-hidden="true">Next</span>
                                </a>
                            </li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </main>
</div>

<!-- JAVASCRIPT FILES -->
<script src="js/jquery.min.js"></script>
<script src="js/bootstrap.bundle.min.js"></script>
<script src="js/apexcharts.min.js"></script>
<script src="js/custom.js"></script>
